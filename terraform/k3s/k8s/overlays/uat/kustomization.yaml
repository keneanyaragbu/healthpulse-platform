apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: -uat

namespace: healthpulse-uat

resources:
  - ../../base

patches:
  - target:
      kind: Namespace
      name: healthpulse
    patch: |-
      - op: replace
        path: /metadata/name
        value: healthpulse-uat
